{
  "name": "reeldock-e2e",
  "private": true,
  "description": "Playwright golden-path E2E against Compose release-smoke stack",
  "scripts": {
    "test": "playwright test --project=chromium",
    "test:extension": "playwright test --project=extension",
    "test:abs": "playwright test --project=abs",
    "test:headed": "playwright test --headed --project=chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
