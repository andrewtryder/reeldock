{
  "name": "reeldock-e2e",
  "private": true,
  "description": "Playwright golden-path E2E against Compose release-smoke stack",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
